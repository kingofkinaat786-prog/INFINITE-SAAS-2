# Infinite SaaS Starter — Ultra Premium

This zip contains a minimal, deployable starter with:
- Vercel serverless endpoints (PayPal, UPI, Stripe samples)
- PayPal webhook verification & reconciliation (server)
- UPI webhook HMAC sample (server)
- SQLite/Postgres helper (fallback)
- AdSense consent component & personal payment links
- Stripe Customer Portal integration (server + client sample)
- Privacy Policy & Terms templates
- Docker + docker-compose for local dev
- scripts/generate-zip.sh helper

**Important**: Set environment variables from `.env.example` before deploying. Do NOT commit secrets to public repos.
